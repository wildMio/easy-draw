import { OptionItemDirective } from './option-item.directive';

describe('OptionItemDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionItemDirective();
    expect(directive).toBeTruthy();
  });
});
