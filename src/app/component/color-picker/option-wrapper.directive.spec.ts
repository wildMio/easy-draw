import { OptionWrapperDirective } from './option-wrapper.directive';

describe('OptionWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
