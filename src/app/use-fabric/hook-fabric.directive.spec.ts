import { HookFabricDirective } from './hook-fabric.directive';

describe('HookFabricDirective', () => {
  it('should create an instance', () => {
    const directive = new HookFabricDirective();
    expect(directive).toBeTruthy();
  });
});
